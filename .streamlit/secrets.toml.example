# 選用功能：「任務詳情／審核」頁的「AI 開場白建議」（services/ai_summary_service.py）。
# 沒有這個檔案／沒有設定這個金鑰時，該功能整個不顯示，不影響其他任何功能。
#
# 使用方式：複製這個檔案為 .streamlit/secrets.toml（會被 .gitignore 排除，不會進版控），
# 填入你自己的 Anthropic API 金鑰。Streamlit Cloud 部署則在 App settings → Secrets 貼上同樣內容。
ANTHROPIC_API_KEY = "sk-ant-..."
